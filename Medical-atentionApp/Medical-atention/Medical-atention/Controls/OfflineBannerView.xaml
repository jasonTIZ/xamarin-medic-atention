<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Medical_atention.Controls.OfflineBannerView"
             x:Name="Root"
             IsVisible="False"
             Opacity="0">

    <Frame x:Name="BannerFrame"
           BackgroundColor="{AppThemeBinding Light=#FFF5F5, Dark=#2D1515}"
           BorderColor="{AppThemeBinding Light=#FC8181, Dark=#E53E3E}"
           CornerRadius="0"
           Padding="12,10"
           HasShadow="False"
           Margin="0">
        <StackLayout Orientation="Horizontal"
                     Spacing="10"
                     HorizontalOptions="Center"
                     VerticalOptions="Center">
            <Label Text="⚠"
                   FontSize="16"
                   TextColor="{StaticResource ErrorDarkColor}"
                   VerticalOptions="Center" />
            <StackLayout Spacing="2" VerticalOptions="Center">
                <Label x:Name="TitleLabel"
                       Text="Sin conexión — datos locales"
                       FontSize="13"
                       FontAttributes="Bold"
                       TextColor="{StaticResource ErrorDarkColor}"
                       HorizontalTextAlignment="Center" />
                <Label x:Name="SyncLabel"
                       FontSize="11"
                       TextColor="#9B2C2C"
                       HorizontalTextAlignment="Center"
                       IsVisible="False" />
            </StackLayout>
        </StackLayout>
    </Frame>

</ContentView>
