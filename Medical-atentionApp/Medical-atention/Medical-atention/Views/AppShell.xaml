<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:Medical_atention.Views"
       x:Class="Medical_atention.Views.AppShell"
       FlyoutBehavior="Flyout"
       Shell.NavBarIsVisible="True">

    <Shell.FlyoutHeader>
        <StackLayout Padding="16,40,16,16" BackgroundColor="#2196F3" Spacing="4">
            <Label x:Name="FlyoutUserName"
                   Text="Usuario"
                   FontSize="18"
                   FontAttributes="Bold"
                   TextColor="White" />
            <Label x:Name="FlyoutUserRole"
                   Text=""
                   FontSize="13"
                   TextColor="#DDEEFF" />
        </StackLayout>
    </Shell.FlyoutHeader>

    <TabBar>
        <ShellContent Title="Pacientes"
                      Icon="👥"
                      ContentTemplate="{DataTemplate views:PatientListPage}"
                      Route="patients" />

        <ShellContent Title="Triaje"
                      Icon="🏥"
                      ContentTemplate="{DataTemplate views:TriagePage}"
                      Route="triage" />

        <ShellContent Title="Perfil"
                      Icon="👤"
                      ContentTemplate="{DataTemplate views:ProfilePage}"
                      Route="profile" />
    </TabBar>

    <MenuItem Text="Cerrar sesión"
              Clicked="OnLogoutClicked" />

</Shell>
